export default function NotFound() {
  return (
    <h1
      className="Not Found"
      style={{
        color: "red",
        padding: "5em",
        textAlign: "center",
        fontSize: "2vw",
      }}
    >
      404 Mistyped URL or a broken link pointing to a non-existent page
    </h1>
  );
}
